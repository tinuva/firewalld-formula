# -*- coding: utf-8 -*-
# vim: ft=yaml
firewalld:
  enabled: true
  package: firewalld
  service: firewalld
  config: /etc/firewalld.conf

  ipset:
    manage: true
    pkg: ipset

  backend:
    manage: true
    pkg: nftables

  ipsets: {}
